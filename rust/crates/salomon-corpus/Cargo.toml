[package]
name = "salomon-corpus"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Bounded corpus manager and deterministic scheduler for SALOMON"

[dependencies]
salomon-core = { path = "../salomon-core" }
